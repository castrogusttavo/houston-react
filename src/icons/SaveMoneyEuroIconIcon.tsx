import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SaveMoneyEuroIconIcon({ iconName = 'save-money-euro-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
