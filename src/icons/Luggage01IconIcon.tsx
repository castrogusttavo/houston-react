import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Luggage01IconIcon({ iconName = 'luggage-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
