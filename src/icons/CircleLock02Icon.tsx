import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLock02Icon({
  iconName = 'circle-lock-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
