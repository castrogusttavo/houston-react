import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliverySecure01Icon({
  iconName = 'delivery-secure-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
