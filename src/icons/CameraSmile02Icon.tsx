import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraSmile02Icon({
  iconName = 'camera-smile-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
