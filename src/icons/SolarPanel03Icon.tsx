import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SolarPanel03Icon({
  iconName = 'solar-panel-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
