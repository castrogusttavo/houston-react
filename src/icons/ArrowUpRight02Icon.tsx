import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUpRight02Icon({
  iconName = 'arrow-up-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
