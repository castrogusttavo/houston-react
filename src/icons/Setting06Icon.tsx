import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Setting06Icon({
  iconName = 'setting-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
