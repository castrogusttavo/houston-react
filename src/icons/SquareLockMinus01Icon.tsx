import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockMinus01Icon({
  iconName = 'square-lock-minus-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
