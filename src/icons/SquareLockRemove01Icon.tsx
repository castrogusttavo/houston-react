import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockRemove01Icon({
  iconName = 'square-lock-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
