import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoreHorizontalSquare01Icon({
  iconName = 'more-horizontal-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
