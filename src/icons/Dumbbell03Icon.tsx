import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Dumbbell03Icon({
  iconName = 'dumbbell-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
