import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraRotated01Icon({
  iconName = 'camera-rotated-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
