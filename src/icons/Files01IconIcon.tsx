import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Files01IconIcon({ iconName = 'files-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
