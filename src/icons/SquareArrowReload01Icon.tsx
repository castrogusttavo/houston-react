import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowReload01Icon({
  iconName = 'square-arrow-reload-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
