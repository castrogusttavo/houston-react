import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Heading04Icon({
  iconName = 'heading-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
