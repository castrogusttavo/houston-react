import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Candelier02Icon({
  iconName = 'candelier-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
