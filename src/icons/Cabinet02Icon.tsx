import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cabinet02Icon({
  iconName = 'cabinet-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
