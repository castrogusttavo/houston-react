import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowUp02Icon({
  iconName = 'square-arrow-up-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
