import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallBlocked02IconIcon({ iconName = 'call-blocked-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
