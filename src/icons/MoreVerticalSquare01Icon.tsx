import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoreVerticalSquare01Icon({
  iconName = 'more-vertical-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
