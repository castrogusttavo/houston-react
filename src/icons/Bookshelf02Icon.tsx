import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Bookshelf02Icon({
  iconName = 'bookshelf-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
