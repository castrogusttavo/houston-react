import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrangeByNumbers19Icon({
  iconName = 'arrange-by-numbers-1-9',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
