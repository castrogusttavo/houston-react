import React from 'react'
import { Icon, IconProps } from '../Icon'

export function StoreRemove02Icon({
  iconName = 'store-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
