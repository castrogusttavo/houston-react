import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Facebook02Icon({
  iconName = 'facebook-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
