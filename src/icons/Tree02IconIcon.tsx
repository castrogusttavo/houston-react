import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tree02IconIcon({ iconName = 'tree-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
