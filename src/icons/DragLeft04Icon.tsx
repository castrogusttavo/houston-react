import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragLeft04Icon({
  iconName = 'drag-left-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
