import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Unlink03Icon({ iconName = 'unlink-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
