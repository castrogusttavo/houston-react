import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreathFirst02IconIcon({ iconName = 'laurel-wreath-first-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
