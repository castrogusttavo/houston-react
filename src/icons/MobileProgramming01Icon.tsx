import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MobileProgramming01Icon({
  iconName = 'mobile-programming-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
