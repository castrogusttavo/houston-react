import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Settings04Icon({
  iconName = 'settings-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
