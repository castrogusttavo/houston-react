import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Contact02Icon({
  iconName = 'contact-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
