import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Key02IconIcon({ iconName = 'key-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
