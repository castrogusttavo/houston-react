import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallUnlocked02Icon({
  iconName = 'call-unlocked-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
