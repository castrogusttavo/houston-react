import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location07Icon({
  iconName = 'location-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
