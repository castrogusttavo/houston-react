import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Beach02Icon({ iconName = 'beach-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
