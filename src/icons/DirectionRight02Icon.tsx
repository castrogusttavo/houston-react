import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DirectionRight02Icon({
  iconName = 'direction-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
