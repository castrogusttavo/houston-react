import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Logout03Icon({ iconName = 'logout-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
