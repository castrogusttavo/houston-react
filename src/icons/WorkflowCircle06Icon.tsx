import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowCircle06Icon({
  iconName = 'workflow-circle-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
