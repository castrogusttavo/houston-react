import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BlushBrush01Icon({
  iconName = 'blush-brush-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
