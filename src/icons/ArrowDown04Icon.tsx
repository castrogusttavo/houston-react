import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDown04Icon({
  iconName = 'arrow-down-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
