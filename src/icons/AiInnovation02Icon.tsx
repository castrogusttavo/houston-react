import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiInnovation02Icon({
  iconName = 'ai-innovation-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
