import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiceFaces01Icon({ iconName = 'dice-faces-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
