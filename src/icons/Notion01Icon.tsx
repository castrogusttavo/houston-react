import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Notion01Icon({ iconName = 'notion-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
