import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiceFaces05IconIcon({ iconName = 'dice-faces-05-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
