import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreathLeft02Icon({ iconName = 'laurel-wreath-left-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
