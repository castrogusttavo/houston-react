import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowExpand02Icon({
  iconName = 'circle-arrow-expand-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
