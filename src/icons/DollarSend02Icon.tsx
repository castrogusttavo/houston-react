import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DollarSend02Icon({
  iconName = 'dollar-send-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
