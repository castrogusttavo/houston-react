import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseRightClick05Icon({
  iconName = 'mouse-right-click-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
