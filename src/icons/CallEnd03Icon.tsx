import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallEnd03Icon({ iconName = 'call-end-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
