import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallEnd03Icon({
  iconName = 'call-end-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
