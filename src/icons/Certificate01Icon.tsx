import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Certificate01Icon({
  iconName = 'certificate-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
