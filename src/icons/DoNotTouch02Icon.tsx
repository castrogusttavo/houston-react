import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DoNotTouch02Icon({
  iconName = 'do-not-touch-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
