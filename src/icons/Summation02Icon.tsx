import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Summation02Icon({
  iconName = 'summation-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
