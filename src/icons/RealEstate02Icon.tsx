import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RealEstate02Icon({
  iconName = 'real-estate-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
