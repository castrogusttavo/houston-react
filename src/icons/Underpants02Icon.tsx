import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Underpants02Icon({
  iconName = 'underpants-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
