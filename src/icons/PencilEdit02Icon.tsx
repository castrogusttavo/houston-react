import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PencilEdit02Icon({
  iconName = 'pencil-edit-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
