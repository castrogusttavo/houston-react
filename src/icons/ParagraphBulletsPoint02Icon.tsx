import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ParagraphBulletsPoint02Icon({
  iconName = 'paragraph-bullets-point-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
