import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowUp01Icon({
  iconName = 'square-arrow-up-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
