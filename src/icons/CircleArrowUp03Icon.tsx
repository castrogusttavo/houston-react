import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowUp03Icon({
  iconName = 'circle-arrow-up-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
