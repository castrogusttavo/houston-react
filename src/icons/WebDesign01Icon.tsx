import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WebDesign01Icon({
  iconName = 'web-design-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
