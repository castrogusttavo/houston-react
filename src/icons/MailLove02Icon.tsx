import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailLove02Icon({
  iconName = 'mail-love-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
