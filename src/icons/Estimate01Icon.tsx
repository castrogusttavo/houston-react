import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Estimate01Icon({
  iconName = 'estimate-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
