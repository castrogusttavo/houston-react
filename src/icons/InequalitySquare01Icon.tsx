import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InequalitySquare01Icon({
  iconName = 'inequality-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
