import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ApproximatelyEqualCircleIcon({
  iconName = 'approximately-equal-circle',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
