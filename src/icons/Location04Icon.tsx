import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location04Icon({
  iconName = 'location-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
