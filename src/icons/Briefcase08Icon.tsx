import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Briefcase08Icon({
  iconName = 'briefcase-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
