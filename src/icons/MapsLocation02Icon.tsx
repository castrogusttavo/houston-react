import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapsLocation02Icon({
  iconName = 'maps-location-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
