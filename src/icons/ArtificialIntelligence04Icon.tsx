import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArtificialIntelligence04Icon({
  iconName = 'artificial-intelligence-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
