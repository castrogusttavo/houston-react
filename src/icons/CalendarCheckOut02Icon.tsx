import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarCheckOut02Icon({
  iconName = 'calendar-check-out-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
