import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WalletRemove02Icon({
  iconName = 'wallet-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
