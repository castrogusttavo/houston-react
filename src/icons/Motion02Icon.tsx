import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Motion02Icon({ iconName = 'motion-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
