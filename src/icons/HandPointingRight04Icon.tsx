import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingRight04Icon({
  iconName = 'hand-pointing-right-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
