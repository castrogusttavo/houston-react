import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiBrain05Icon({
  iconName = 'ai-brain-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
