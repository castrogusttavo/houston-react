import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreathFirst01Icon({
  iconName = 'laurel-wreath-first-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
