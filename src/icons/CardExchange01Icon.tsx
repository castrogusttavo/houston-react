import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CardExchange01Icon({
  iconName = 'card-exchange-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
