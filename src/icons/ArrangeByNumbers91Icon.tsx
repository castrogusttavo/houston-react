import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrangeByNumbers91Icon({
  iconName = 'arrange-by-numbers-9-1',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
