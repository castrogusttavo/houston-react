import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mosque02Icon({ iconName = 'mosque-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
