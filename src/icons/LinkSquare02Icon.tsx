import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LinkSquare02Icon({
  iconName = 'link-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
