import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketAdd03Icon({
  iconName = 'shopping-basket-add-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
