import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AccountSetting03Icon({
  iconName = 'account-setting-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
