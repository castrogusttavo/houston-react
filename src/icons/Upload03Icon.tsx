import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Upload03Icon({ iconName = 'upload-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
