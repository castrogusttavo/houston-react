import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapsCircle01Icon({
  iconName = 'maps-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
