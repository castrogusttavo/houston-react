import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Backward02Icon({
  iconName = 'backward-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
