import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CustomerService02Icon({
  iconName = 'customer-service-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
