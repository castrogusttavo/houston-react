import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Minimize04Icon({
  iconName = 'minimize-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
