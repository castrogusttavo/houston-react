import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cosine02Icon({ iconName = 'cosine-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
