import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Pickup02Icon({ iconName = 'pickup-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
