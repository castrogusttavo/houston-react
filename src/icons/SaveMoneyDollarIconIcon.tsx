import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SaveMoneyDollarIconIcon({ iconName = 'save-money-dollar-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
