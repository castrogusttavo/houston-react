import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarDownload01Icon({
  iconName = 'calendar-download-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
