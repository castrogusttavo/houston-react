import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudAngledRain01Icon({
  iconName = 'sun-cloud-angled-rain-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
