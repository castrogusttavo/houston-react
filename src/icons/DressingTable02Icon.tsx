import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DressingTable02Icon({
  iconName = 'dressing-table-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
