import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailRemove01Icon({
  iconName = 'mail-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
