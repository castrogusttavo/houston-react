import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DressingTable01Icon({
  iconName = 'dressing-table-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
