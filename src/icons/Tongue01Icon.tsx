import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tongue01Icon({ iconName = 'tongue-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
