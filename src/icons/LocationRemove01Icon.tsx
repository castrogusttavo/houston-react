import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationRemove01Icon({
  iconName = 'location-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
