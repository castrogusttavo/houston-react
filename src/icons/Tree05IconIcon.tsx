import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tree05IconIcon({ iconName = 'tree-05-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
