import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingLeft02Icon({
  iconName = 'hand-pointing-left-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
