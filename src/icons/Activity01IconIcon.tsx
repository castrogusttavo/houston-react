import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Activity01IconIcon({ iconName = 'activity-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
