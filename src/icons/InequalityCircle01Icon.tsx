import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InequalityCircle01Icon({
  iconName = 'inequality-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
