import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ThreeFinger05Icon({
  iconName = 'three-finger-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
