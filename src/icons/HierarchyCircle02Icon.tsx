import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchyCircle02Icon({
  iconName = 'hierarchy-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
