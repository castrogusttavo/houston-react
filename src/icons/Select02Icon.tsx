import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Select02Icon({ iconName = 'select-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
