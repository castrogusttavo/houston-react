import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BbqGrillIcon({ iconName = 'bbq-grill', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
