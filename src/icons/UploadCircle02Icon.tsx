import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UploadCircle02Icon({
  iconName = 'upload-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
