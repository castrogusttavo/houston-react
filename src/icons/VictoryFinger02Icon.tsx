import React from 'react'
import { Icon, IconProps } from '../Icon'

export function VictoryFinger02Icon({
  iconName = 'victory-finger-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
