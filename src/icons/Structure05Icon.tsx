import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Structure05Icon({
  iconName = 'structure-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
