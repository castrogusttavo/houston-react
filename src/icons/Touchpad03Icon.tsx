import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Touchpad03Icon({
  iconName = 'touchpad-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
