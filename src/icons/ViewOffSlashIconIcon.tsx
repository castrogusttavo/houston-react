import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ViewOffSlashIconIcon({ iconName = 'view-off-slash-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
