import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SaveMoneyYenIconIcon({ iconName = 'save-money-yen-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
