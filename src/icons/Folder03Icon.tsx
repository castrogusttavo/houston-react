import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Folder03Icon({ iconName = 'folder-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
