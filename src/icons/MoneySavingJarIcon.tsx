import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneySavingJarIcon({ iconName = 'money-saving-jar-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
