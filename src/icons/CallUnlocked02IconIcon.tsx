import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallUnlocked02IconIcon({ iconName = 'call-unlocked-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
