import React from 'react'
import { Icon, IconProps } from '../Icon'

export function NotificationSnooze02Icon({
  iconName = 'notification-snooze-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
