import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallEnd04IconIcon({ iconName = 'call-end-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
