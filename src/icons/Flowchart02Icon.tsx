import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Flowchart02Icon({
  iconName = 'flowchart-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
