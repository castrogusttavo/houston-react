import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RssConnected02Icon({
  iconName = 'rss-connected-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
