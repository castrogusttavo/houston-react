import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallMissed04Icon({
  iconName = 'call-missed-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
