import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketDone02Icon({
  iconName = 'shopping-basket-done-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
