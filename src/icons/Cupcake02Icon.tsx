import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cupcake02Icon({
  iconName = 'cupcake-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
