import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Plant03Icon({ iconName = 'plant-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
