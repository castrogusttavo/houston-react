import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DownloadCircle02Icon({
  iconName = 'download-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
