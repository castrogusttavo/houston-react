import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft04Icon({
  iconName = 'pointing-left-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
