import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Saturn02Icon({ iconName = 'saturn-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
