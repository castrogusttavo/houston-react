import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallOutgoing03IconIcon({ iconName = 'call-outgoing-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
