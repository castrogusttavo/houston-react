import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ImageDownload02Icon({
  iconName = 'image-download-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
