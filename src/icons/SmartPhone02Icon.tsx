import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SmartPhone02Icon({
  iconName = 'smart-phone-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
