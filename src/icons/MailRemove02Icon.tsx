import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailRemove02Icon({
  iconName = 'mail-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
