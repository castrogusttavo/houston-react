import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Layout04Icon({ iconName = 'layout-04', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
