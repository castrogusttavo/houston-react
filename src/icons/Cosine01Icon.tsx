import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cosine01Icon({ iconName = 'cosine-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
