import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageFavourite02Icon({
  iconName = 'message-favourite-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
