import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderBottom01Icon({
  iconName = 'border-bottom-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
