import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ImageRemove02Icon({
  iconName = 'image-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
