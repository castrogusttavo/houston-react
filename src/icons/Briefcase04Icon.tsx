import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Briefcase04Icon({
  iconName = 'briefcase-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
