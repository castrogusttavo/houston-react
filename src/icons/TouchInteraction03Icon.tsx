import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TouchInteraction03Icon({
  iconName = 'touch-interaction-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
