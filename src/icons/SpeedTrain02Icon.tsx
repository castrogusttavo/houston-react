import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SpeedTrain02Icon({
  iconName = 'speed-train-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
