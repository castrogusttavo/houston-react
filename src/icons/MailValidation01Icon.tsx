import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailValidation01Icon({
  iconName = 'mail-validation-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
