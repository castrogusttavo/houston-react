import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Payment01IconIcon({ iconName = 'payment-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
