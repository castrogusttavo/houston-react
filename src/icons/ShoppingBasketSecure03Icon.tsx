import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketSecure03Icon({
  iconName = 'shopping-basket-secure-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
