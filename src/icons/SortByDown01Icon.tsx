import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SortByDown01Icon({
  iconName = 'sort-by-down-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
