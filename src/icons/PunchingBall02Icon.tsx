import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PunchingBall02Icon({
  iconName = 'punching-ball-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
