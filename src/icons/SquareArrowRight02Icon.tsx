import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowRight02Icon({
  iconName = 'square-arrow-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
