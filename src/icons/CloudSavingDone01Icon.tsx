import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CloudSavingDone01Icon({
  iconName = 'cloud-saving-done-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
