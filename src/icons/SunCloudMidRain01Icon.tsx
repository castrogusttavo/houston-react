import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudMidRain01Icon({
  iconName = 'sun-cloud-mid-rain-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
