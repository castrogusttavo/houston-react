import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorRemoveSelection02Icon({
  iconName = 'cursor-remove-selection-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
