import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Layers01Icon({ iconName = 'layers-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
