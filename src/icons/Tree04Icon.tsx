import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tree04Icon({ iconName = 'tree-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
