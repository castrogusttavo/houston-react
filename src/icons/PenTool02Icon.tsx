import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PenTool02Icon({
  iconName = 'pen-tool-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
