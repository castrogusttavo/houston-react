import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ResetPasswordIconIcon({ iconName = 'reset-password-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
