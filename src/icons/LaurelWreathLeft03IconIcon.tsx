import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreathLeft03IconIcon({ iconName = 'laurel-wreath-left-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
