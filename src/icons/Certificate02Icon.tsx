import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Certificate02Icon({
  iconName = 'certificate-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
