import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapsSquare02Icon({
  iconName = 'maps-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
