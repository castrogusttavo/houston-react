import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowExpand02Icon({
  iconName = 'arrow-expand-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
