import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LoginCircle02Icon({
  iconName = 'login-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
