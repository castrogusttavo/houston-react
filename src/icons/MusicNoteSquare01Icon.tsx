import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MusicNoteSquare01Icon({
  iconName = 'music-note-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
