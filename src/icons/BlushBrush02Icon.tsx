import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BlushBrush02Icon({
  iconName = 'blush-brush-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
