import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DialpadCircle02Icon({
  iconName = 'dialpad-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
