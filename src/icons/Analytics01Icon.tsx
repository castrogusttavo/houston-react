import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Analytics01Icon({
  iconName = 'analytics-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
