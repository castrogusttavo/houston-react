import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarRemove02Icon({
  iconName = 'calendar-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
