import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Billiard02Icon({
  iconName = 'billiard-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
