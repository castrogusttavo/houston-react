import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Navigation04Icon({
  iconName = 'navigation-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
