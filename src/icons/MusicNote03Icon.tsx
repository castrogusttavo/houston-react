import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MusicNote03Icon({
  iconName = 'music-note-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
