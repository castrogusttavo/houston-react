import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mosque04Icon({ iconName = 'mosque-04', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
