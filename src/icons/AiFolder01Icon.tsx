import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiFolder01Icon({
  iconName = 'ai-folder-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
