import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SchoolBell02Icon({
  iconName = 'school-bell-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
