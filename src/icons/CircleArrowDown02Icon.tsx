import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDown02Icon({
  iconName = 'circle-arrow-down-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
