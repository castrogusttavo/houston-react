import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ReverseWithdrawal02Icon({ iconName = 'reverse-withdrawal-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
