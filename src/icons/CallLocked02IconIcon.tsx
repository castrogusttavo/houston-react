import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallLocked02IconIcon({ iconName = 'call-locked-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
