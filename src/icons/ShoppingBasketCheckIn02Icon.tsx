import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketCheckIn02Icon({
  iconName = 'shopping-basket-check-in-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
