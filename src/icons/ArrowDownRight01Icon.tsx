import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDownRight01Icon({
  iconName = 'arrow-down-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
