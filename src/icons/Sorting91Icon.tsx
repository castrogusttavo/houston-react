import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Sorting91Icon({
  iconName = 'sorting-9-1',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
