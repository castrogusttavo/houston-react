import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiceFaces06IconIcon({ iconName = 'dice-faces-06-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
