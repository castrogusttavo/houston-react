import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RssConnected01Icon({
  iconName = 'rss-connected-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
