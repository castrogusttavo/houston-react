import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RiceBowl02Icon({
  iconName = 'rice-bowl-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
