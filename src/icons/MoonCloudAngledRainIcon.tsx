import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoonCloudAngledRainIcon({
  iconName = 'moon-cloud-angled-rain',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
