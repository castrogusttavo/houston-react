import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorMove02Icon({
  iconName = 'cursor-move-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
