import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoreHorizontalSquare02Icon({
  iconName = 'more-horizontal-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
