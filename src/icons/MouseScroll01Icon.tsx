import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseScroll01Icon({
  iconName = 'mouse-scroll-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
