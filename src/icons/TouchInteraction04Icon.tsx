import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TouchInteraction04Icon({
  iconName = 'touch-interaction-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
