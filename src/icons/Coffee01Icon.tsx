import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Coffee01Icon({ iconName = 'coffee-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
