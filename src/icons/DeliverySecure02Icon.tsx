import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliverySecure02Icon({
  iconName = 'delivery-secure-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
