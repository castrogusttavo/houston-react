import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookBookmark01Icon({
  iconName = 'book-bookmark-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
