import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiceFaces04IconIcon({ iconName = 'dice-faces-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
