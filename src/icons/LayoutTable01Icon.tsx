import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LayoutTable01Icon({
  iconName = 'layout-table-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
