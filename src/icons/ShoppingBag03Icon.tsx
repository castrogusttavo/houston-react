import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBag03Icon({
  iconName = 'shopping-bag-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
