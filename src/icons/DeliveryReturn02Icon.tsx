import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryReturn02Icon({
  iconName = 'delivery-return-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
