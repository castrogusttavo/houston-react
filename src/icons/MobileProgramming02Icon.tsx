import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MobileProgramming02Icon({
  iconName = 'mobile-programming-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
