import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Leaf02Icon({ iconName = 'leaf-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
