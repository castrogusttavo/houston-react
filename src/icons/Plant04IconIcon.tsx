import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Plant04IconIcon({ iconName = 'plant-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
