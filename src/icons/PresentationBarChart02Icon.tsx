import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PresentationBarChart02Icon({
  iconName = 'presentation-bar-chart-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
