import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InternetAntenna01Icon({
  iconName = 'internet-antenna-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
