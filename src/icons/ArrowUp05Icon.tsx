import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUp05Icon({
  iconName = 'arrow-up-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
