import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DashboardSpeed01Icon({
  iconName = 'dashboard-speed-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
