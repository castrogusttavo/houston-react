import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Home08Icon({ iconName = 'home-08-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
