import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GeometricShapes02Icon({
  iconName = 'geometric-shapes-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
