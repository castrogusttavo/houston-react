import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorRectangleSelection02Icon({
  iconName = 'cursor-rectangle-selection-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
