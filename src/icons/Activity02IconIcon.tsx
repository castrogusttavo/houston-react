import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Activity02IconIcon({ iconName = 'activity-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
