import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDataTransferDiagonalIcon({
  iconName = 'square-arrow-data-transfer-diagonal',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
