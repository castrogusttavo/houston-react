import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapPinpoint02Icon({
  iconName = 'map-pinpoint-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
