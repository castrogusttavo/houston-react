import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryReturn01Icon({
  iconName = 'delivery-return-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
