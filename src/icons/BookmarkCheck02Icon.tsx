import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkCheck02Icon({
  iconName = 'bookmark-check-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
