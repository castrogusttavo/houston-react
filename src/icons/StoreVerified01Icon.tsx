import React from 'react'
import { Icon, IconProps } from '../Icon'

export function StoreVerified01Icon({
  iconName = 'store-verified-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
