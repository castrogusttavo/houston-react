import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Unlink01Icon({ iconName = 'unlink-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
