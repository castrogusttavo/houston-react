import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RecoveryMailIconIcon({ iconName = 'recovery-mail-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
