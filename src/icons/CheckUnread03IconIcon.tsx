import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckUnread03IconIcon({ iconName = 'check-unread-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
