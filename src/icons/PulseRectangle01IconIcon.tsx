import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PulseRectangle01IconIcon({ iconName = 'pulse-rectangle-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
