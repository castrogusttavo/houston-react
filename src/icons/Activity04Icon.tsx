import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Activity04Icon({
  iconName = 'activity-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
