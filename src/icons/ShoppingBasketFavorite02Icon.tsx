import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketFavorite02Icon({
  iconName = 'shopping-basket-favorite-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
