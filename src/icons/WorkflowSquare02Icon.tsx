import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare02Icon({
  iconName = 'workflow-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
