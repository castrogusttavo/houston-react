import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Scooter04Icon({
  iconName = 'scooter-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
