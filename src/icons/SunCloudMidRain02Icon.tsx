import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudMidRain02Icon({
  iconName = 'sun-cloud-mid-rain-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
