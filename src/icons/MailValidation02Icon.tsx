import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailValidation02Icon({
  iconName = 'mail-validation-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
