import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SortingAZ01Icon({
  iconName = 'sorting-a-z-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
