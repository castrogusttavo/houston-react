import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailUpload01Icon({
  iconName = 'mail-upload-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
