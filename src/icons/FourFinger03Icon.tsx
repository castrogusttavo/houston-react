import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FourFinger03Icon({
  iconName = 'four-finger-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
