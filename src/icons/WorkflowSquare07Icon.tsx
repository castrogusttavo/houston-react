import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare07Icon({
  iconName = 'workflow-square-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
