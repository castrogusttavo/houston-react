import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft06Icon({
  iconName = 'pointing-left-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
