import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowCircle05Icon({
  iconName = 'workflow-circle-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
