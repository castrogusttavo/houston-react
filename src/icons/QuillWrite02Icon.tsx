import React from 'react'
import { Icon, IconProps } from '../Icon'

export function QuillWrite02Icon({
  iconName = 'quill-write-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
