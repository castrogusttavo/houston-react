import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkOff01Icon({
  iconName = 'bookmark-off-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
