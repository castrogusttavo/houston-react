import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailSetting02Icon({
  iconName = 'mail-setting-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
