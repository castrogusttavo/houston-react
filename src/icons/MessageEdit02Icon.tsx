import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageEdit02Icon({
  iconName = 'message-edit-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
