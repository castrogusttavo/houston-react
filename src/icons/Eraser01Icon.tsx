import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Eraser01Icon({ iconName = 'eraser-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
