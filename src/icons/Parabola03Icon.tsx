import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Parabola03Icon({
  iconName = 'parabola-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
