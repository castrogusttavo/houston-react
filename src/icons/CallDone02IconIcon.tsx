import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallDone02IconIcon({ iconName = 'call-done-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
