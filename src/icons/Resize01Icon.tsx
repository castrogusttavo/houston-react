import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Resize01Icon({ iconName = 'resize-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
