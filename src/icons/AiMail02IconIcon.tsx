import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiMail02IconIcon({ iconName = 'ai-mail-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
