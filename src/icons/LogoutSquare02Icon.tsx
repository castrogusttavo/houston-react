import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LogoutSquare02Icon({
  iconName = 'logout-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
