import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderLeft01Icon({
  iconName = 'border-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
