import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudLittleSnow02Icon({
  iconName = 'sun-cloud-little-snow-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
