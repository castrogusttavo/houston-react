import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Search02Icon({ iconName = 'search-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
