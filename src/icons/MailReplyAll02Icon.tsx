import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailReplyAll02Icon({
  iconName = 'mail-reply-all-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
