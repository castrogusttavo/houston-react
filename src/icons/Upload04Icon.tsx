import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Upload04Icon({ iconName = 'upload-04', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
