import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartRemove02Icon({
  iconName = 'shopping-cart-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
