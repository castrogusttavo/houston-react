import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Leaf02IconIcon({ iconName = 'leaf-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
