import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Plant01IconIcon({ iconName = 'plant-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
