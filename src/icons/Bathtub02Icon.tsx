import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Bathtub02Icon({
  iconName = 'bathtub-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
