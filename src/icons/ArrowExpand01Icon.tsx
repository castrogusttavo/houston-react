import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowExpand01Icon({
  iconName = 'arrow-expand-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
