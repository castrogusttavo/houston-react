import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeUp05Icon({
  iconName = 'swipe-up-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
