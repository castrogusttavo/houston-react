import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapsSquare01Icon({
  iconName = 'maps-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
