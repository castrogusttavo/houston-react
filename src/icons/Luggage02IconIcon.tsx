import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Luggage02IconIcon({ iconName = 'luggage-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
