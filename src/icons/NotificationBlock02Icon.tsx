import React from 'react'
import { Icon, IconProps } from '../Icon'

export function NotificationBlock02Icon({
  iconName = 'notification-block-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
