import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudAngledRainZap01Icon({
  iconName = 'sun-cloud-angled-rain-zap-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
