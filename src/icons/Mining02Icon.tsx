import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mining02Icon({ iconName = 'mining-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
