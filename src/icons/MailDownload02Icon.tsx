import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailDownload02Icon({
  iconName = 'mail-download-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
