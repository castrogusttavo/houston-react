import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DialpadSquare02Icon({
  iconName = 'dialpad-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
