import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Linkedin02Icon({
  iconName = 'linkedin-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
