import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PrayerRug01Icon({
  iconName = 'prayer-rug-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
