import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreath01Icon({
  iconName = 'laurel-wreath-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
