import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketCheckIn03Icon({
  iconName = 'shopping-basket-check-in-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
