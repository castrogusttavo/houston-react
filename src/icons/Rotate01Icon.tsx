import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Rotate01Icon({ iconName = 'rotate-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
