import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Disability01Icon({
  iconName = 'disability-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
