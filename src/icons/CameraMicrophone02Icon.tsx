import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraMicrophone02Icon({
  iconName = 'camera-microphone-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
