import React from 'react'
import { Icon, IconProps } from '../Icon'

export function NotificationOff02Icon({
  iconName = 'notification-off-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
