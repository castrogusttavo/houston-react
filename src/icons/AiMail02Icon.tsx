import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiMail02Icon({ iconName = 'ai-mail-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
