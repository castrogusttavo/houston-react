import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Locker01Icon({ iconName = 'locker-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
