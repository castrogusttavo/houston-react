import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TwoFinger03Icon({
  iconName = 'two-finger-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
