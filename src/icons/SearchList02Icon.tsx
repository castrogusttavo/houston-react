import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SearchList02Icon({
  iconName = 'search-list-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
