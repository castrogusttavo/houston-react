import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CommentBlock02Icon({
  iconName = 'comment-block-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
