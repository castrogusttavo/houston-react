import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft07Icon({
  iconName = 'pointing-left-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
