import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Shield02Icon({ iconName = 'shield-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
