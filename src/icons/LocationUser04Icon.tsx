import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationUser04Icon({
  iconName = 'location-user-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
