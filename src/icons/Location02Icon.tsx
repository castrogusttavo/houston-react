import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location02Icon({
  iconName = 'location-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
