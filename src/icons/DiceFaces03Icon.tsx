import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiceFaces03Icon({ iconName = 'dice-faces-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
