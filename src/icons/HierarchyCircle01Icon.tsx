import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchyCircle01Icon({
  iconName = 'hierarchy-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
