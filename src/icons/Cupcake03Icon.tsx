import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cupcake03Icon({
  iconName = 'cupcake-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
