import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DownloadCircle01Icon({
  iconName = 'download-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
