import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Shield02IconIcon({ iconName = 'shield-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
