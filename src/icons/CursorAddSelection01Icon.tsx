import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorAddSelection01Icon({
  iconName = 'cursor-add-selection-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
