import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Bookshelf03Icon({
  iconName = 'bookshelf-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
