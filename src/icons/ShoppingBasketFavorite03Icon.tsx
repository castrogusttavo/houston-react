import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketFavorite03Icon({
  iconName = 'shopping-basket-favorite-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
