import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ImageNotFound02Icon({
  iconName = 'image-not-found-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
