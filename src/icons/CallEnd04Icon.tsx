import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallEnd04Icon({
  iconName = 'call-end-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
