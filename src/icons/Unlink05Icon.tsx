import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Unlink05Icon({ iconName = 'unlink-05', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
