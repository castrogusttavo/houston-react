import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Luggage02Icon({
  iconName = 'luggage-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
