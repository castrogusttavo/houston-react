import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudAngledZap01Icon({
  iconName = 'sun-cloud-angled-zap-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
