import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockMinus01Icon({
  iconName = 'circle-lock-minus-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
