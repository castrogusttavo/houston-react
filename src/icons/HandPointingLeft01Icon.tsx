import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingLeft01Icon({
  iconName = 'hand-pointing-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
