import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CloudSavingDone02Icon({
  iconName = 'cloud-saving-done-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
