import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderRight01Icon({
  iconName = 'border-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
