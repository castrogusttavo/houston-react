import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SaveEnergy02Icon({
  iconName = 'save-energy-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
