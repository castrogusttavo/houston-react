import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tree06Icon({ iconName = 'tree-06-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
