import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookBookmark02Icon({
  iconName = 'book-bookmark-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
