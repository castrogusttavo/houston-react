import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Joystick05Icon({
  iconName = 'joystick-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
