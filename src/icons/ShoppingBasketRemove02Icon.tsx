import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketRemove02Icon({
  iconName = 'shopping-basket-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
