import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare02Icon({
  iconName = 'hierarchy-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
