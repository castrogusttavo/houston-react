import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Home09Icon({ iconName = 'home-09-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
