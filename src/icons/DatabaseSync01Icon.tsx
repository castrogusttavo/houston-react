import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DatabaseSync01Icon({
  iconName = 'database-sync-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
