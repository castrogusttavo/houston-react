import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateLeft04Icon({
  iconName = 'rotate-left-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
