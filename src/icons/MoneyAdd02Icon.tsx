import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyAdd02Icon({
  iconName = 'money-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
