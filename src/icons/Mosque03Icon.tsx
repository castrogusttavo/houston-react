import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mosque03Icon({ iconName = 'mosque-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
