import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreathFirst02Icon({ iconName = 'laurel-wreath-first-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
