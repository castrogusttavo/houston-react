import React from 'react'
import { Icon, IconProps } from '../Icon'

export function JusticeScale02Icon({
  iconName = 'justice-scale-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
