import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GoogleGeminiIcon({ iconName = 'google-gemini-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
