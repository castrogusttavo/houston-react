import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Rocket02Icon({ iconName = 'rocket-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
