import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiscountTag02Icon({
  iconName = 'discount-tag-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
