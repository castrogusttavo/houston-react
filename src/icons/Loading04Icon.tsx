import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Loading04Icon({
  iconName = 'loading-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
