import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShareLocation02Icon({
  iconName = 'share-location-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
