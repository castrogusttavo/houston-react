import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarDownload02Icon({
  iconName = 'calendar-download-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
