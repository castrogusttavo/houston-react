import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CommentRemove02Icon({
  iconName = 'comment-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
