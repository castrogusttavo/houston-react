import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Dumbbell02Icon({
  iconName = 'dumbbell-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
