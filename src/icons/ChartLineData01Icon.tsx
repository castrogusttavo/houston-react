import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ChartLineData01Icon({
  iconName = 'chart-line-data-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
