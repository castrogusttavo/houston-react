import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailEdit02Icon({
  iconName = 'mail-edit-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
