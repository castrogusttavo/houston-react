import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDataTransferVerticalIcon({
  iconName = 'arrow-data-transfer-vertical',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
