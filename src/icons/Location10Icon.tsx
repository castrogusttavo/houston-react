import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location10Icon({
  iconName = 'location-10',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
