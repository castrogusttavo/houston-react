import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkAdd02Icon({
  iconName = 'bookmark-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
