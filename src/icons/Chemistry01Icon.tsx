import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Chemistry01Icon({
  iconName = 'chemistry-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
