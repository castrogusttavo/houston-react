import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UserLove02Icon({
  iconName = 'user-love-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
