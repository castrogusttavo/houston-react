import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailReplyAll01Icon({
  iconName = 'mail-reply-all-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
