import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreath02IconIcon({ iconName = 'laurel-wreath-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
