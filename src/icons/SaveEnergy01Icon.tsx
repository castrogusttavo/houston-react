import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SaveEnergy01Icon({
  iconName = 'save-energy-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
