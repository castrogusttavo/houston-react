import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Upload02Icon({ iconName = 'upload-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
