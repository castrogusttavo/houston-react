import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Magnet02Icon({ iconName = 'magnet-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
