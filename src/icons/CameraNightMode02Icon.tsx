import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraNightMode02Icon({
  iconName = 'camera-night-mode-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
