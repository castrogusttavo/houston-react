import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InsertColumnRightIconIcon({ iconName = 'insert-column-right-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
