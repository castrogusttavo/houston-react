import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Wardrobe04Icon({
  iconName = 'wardrobe-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
