import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraNightMode01Icon({
  iconName = 'camera-night-mode-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
