import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreath01IconIcon({ iconName = 'laurel-wreath-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
