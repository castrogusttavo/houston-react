import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Sorting01Icon({
  iconName = 'sorting-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
