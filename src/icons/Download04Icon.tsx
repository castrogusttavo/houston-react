import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Download04Icon({
  iconName = 'download-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
