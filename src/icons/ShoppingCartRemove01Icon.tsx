import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartRemove01Icon({
  iconName = 'shopping-cart-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
