import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LassoTool02Icon({
  iconName = 'lasso-tool-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
