import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Ticket02Icon({ iconName = 'ticket-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
