import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Database02Icon({
  iconName = 'database-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
