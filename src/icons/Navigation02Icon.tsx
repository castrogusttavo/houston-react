import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Navigation02Icon({
  iconName = 'navigation-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
