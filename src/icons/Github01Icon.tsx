import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Github01Icon({ iconName = 'github-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
