import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Joystick03Icon({
  iconName = 'joystick-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
