import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location09Icon({
  iconName = 'location-09',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
