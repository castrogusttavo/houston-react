import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GeometricShapes01Icon({
  iconName = 'geometric-shapes-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
