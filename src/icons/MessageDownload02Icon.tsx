import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageDownload02Icon({
  iconName = 'message-download-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
