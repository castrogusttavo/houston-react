import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Layout06Icon({ iconName = 'layout-06', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
