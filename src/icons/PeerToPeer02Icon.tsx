import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PeerToPeer02Icon({
  iconName = 'peer-to-peer-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
