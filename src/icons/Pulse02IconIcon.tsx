import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Pulse02IconIcon({ iconName = 'pulse-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
