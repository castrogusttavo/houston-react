import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUpRight01Icon({
  iconName = 'arrow-up-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
