import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowExpandDiagonal02Icon({
  iconName = 'arrow-expand-diagonal-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
