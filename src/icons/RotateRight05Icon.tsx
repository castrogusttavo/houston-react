import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateRight05Icon({
  iconName = 'rotate-right-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
