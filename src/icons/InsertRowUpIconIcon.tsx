import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InsertRowUpIconIcon({ iconName = 'insert-row-up-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
