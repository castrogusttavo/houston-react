import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeUp04Icon({
  iconName = 'swipe-up-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
