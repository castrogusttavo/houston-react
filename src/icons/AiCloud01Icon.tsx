import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiCloud01Icon({
  iconName = 'ai-cloud-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
