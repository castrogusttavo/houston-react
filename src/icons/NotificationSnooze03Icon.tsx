import React from 'react'
import { Icon, IconProps } from '../Icon'

export function NotificationSnooze03Icon({
  iconName = 'notification-snooze-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
