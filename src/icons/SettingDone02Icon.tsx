import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SettingDone02Icon({
  iconName = 'setting-done-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
