import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockMinus02Icon({
  iconName = 'square-lock-minus-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
