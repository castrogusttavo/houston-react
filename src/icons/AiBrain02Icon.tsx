import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiBrain02Icon({
  iconName = 'ai-brain-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
