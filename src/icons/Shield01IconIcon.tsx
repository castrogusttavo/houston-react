import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Shield01IconIcon({ iconName = 'shield-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
