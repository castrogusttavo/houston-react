import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudLittleRain02Icon({
  iconName = 'sun-cloud-little-rain-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
