import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarRemove01Icon({
  iconName = 'calendar-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
