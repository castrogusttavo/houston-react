import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareUnlock01Icon({
  iconName = 'square-unlock-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
