import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Activity02Icon({ iconName = 'activity-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
