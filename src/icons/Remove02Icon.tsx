import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Remove02Icon({ iconName = 'remove-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
