import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HospitalBed02Icon({
  iconName = 'hospital-bed-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
