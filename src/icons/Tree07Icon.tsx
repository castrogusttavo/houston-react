import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tree07Icon({ iconName = 'tree-07-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
