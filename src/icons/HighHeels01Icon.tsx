import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HighHeels01Icon({
  iconName = 'high-heels-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
