import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketCheckOut01Icon({
  iconName = 'shopping-basket-check-out-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
