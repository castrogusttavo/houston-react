import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown07Icon({
  iconName = 'swipe-down-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
