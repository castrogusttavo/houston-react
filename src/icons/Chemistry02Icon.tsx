import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Chemistry02Icon({
  iconName = 'chemistry-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
