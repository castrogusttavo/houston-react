import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Ramadhan02Icon({
  iconName = 'ramadhan-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
