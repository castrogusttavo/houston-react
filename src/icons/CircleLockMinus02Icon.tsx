import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockMinus02Icon({
  iconName = 'circle-lock-minus-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
