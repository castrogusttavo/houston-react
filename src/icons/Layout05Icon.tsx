import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Layout05Icon({ iconName = 'layout-05', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
