import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PresentationBarChart01Icon({
  iconName = 'presentation-bar-chart-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
