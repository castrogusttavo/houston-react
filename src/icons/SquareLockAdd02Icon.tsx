import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockAdd02Icon({
  iconName = 'square-lock-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
