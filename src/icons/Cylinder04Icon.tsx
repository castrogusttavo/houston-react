import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cylinder04Icon({
  iconName = 'cylinder-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
