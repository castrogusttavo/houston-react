import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowShrink02Icon({
  iconName = 'circle-arrow-shrink-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
