import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare01Icon({
  iconName = 'hierarchy-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
