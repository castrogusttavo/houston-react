import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SortByDown02Icon({
  iconName = 'sort-by-down-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
