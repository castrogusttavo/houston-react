import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDataTransferHorizontalIcon({
  iconName = 'square-arrow-data-transfer-horizontal',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
