import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MedicineBottle02Icon({
  iconName = 'medicine-bottle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
