import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallRinging03Icon({ iconName = 'call-ringing-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
