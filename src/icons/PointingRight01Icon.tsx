import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight01Icon({
  iconName = 'pointing-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
