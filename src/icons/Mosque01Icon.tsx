import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mosque01Icon({ iconName = 'mosque-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
