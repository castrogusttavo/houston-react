import React from 'react'
import { Icon, IconProps } from '../Icon'

export function StoreAdd02Icon({
  iconName = 'store-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
