import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Kettle01Icon({ iconName = 'kettle-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
