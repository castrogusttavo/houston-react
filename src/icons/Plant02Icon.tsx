import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Plant02Icon({ iconName = 'plant-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
