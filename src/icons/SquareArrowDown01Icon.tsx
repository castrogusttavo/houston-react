import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDown01Icon({
  iconName = 'square-arrow-down-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
