import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DashboardSpeed02Icon({
  iconName = 'dashboard-speed-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
