import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationUser02Icon({
  iconName = 'location-user-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
