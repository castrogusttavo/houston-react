import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Attachment02Icon({
  iconName = 'attachment-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
