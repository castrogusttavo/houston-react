import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InstallingUpdates01Icon({
  iconName = 'installing-updates-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
