import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Underpants03Icon({
  iconName = 'underpants-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
