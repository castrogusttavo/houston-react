import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Fuel01IconIcon({ iconName = 'fuel-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
