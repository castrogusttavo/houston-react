import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketRemove03Icon({
  iconName = 'shopping-basket-remove-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
