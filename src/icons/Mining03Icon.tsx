import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mining03Icon({ iconName = 'mining-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
