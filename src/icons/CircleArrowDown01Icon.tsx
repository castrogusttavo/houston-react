import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDown01Icon({
  iconName = 'circle-arrow-down-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
