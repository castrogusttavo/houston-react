import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare10Icon({
  iconName = 'workflow-square-10',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
