import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WifiDisconnected03Icon({
  iconName = 'wifi-disconnected-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
