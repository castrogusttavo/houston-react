import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DirectionLeft02Icon({
  iconName = 'direction-left-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
