import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PeerToPeer01Icon({
  iconName = 'peer-to-peer-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
