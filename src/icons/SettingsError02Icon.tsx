import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SettingsError02Icon({
  iconName = 'settings-error-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
