import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDownLeft01Icon({
  iconName = 'arrow-down-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
