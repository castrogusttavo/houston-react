import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cancel02Icon({ iconName = 'cancel-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
