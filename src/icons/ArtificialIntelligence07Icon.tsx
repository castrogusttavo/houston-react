import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArtificialIntelligence07Icon({
  iconName = 'artificial-intelligence-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
