import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryDelay01Icon({
  iconName = 'delivery-delay-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
