import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUpLeft01Icon({
  iconName = 'arrow-up-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
