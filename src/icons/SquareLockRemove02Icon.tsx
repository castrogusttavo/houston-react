import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockRemove02Icon({
  iconName = 'square-lock-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
