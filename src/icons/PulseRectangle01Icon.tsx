import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PulseRectangle01Icon({
  iconName = 'pulse-rectangle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
