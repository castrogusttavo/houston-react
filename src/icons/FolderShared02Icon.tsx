import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FolderShared02Icon({
  iconName = 'folder-shared-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
