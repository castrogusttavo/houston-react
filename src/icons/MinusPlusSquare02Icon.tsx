import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MinusPlusSquare02Icon({
  iconName = 'minus-plus-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
