import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PhoneOff02Icon({
  iconName = 'phone-off-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
