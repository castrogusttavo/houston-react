import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowRight03Icon({
  iconName = 'circle-arrow-right-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
