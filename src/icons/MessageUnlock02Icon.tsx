import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageUnlock02Icon({
  iconName = 'message-unlock-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
