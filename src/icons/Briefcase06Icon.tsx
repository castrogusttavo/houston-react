import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Briefcase06Icon({
  iconName = 'briefcase-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
