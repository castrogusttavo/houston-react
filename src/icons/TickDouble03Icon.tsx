import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TickDouble03Icon({
  iconName = 'tick-double-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
