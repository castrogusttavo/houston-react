import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowReload01Icon({
  iconName = 'circle-arrow-reload-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
