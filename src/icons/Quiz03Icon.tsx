import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Quiz03Icon({ iconName = 'quiz-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
