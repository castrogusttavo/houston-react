import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Coupon02Icon({ iconName = 'coupon-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
