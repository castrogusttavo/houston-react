import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Logout04Icon({ iconName = 'logout-04', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
