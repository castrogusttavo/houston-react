import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TwoFinger02Icon({
  iconName = 'two-finger-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
