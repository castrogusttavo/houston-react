import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InsertColumnLeftIconIcon({ iconName = 'insert-column-left-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
