import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingDown02Icon({
  iconName = 'hand-pointing-down-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
