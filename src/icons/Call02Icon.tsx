import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Call02Icon({ iconName = 'call-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
