import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiChat01Icon({ iconName = 'ai-chat-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
