import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkRemove02Icon({
  iconName = 'bookmark-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
