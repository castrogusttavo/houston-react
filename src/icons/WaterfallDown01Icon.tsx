import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WaterfallDown01Icon({
  iconName = 'waterfall-down-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
