import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingRight03Icon({
  iconName = 'hand-pointing-right-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
