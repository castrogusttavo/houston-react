import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLockAdd01Icon({
  iconName = 'square-lock-add-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
