import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Presentation03Icon({
  iconName = 'presentation-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
