import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorAddSelection02Icon({
  iconName = 'cursor-add-selection-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
