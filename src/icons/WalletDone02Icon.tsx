import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WalletDone02Icon({
  iconName = 'wallet-done-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
