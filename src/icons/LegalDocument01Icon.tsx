import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LegalDocument01Icon({
  iconName = 'legal-document-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
