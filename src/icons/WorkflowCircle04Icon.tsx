import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowCircle04Icon({
  iconName = 'workflow-circle-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
