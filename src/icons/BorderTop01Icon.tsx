import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderTop01Icon({
  iconName = 'border-top-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
