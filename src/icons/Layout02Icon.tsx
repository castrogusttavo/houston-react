import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Layout02Icon({ iconName = 'layout-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
