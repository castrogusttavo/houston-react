import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mosque05Icon({ iconName = 'mosque-05', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
