import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Recycle03Icon({
  iconName = 'recycle-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
