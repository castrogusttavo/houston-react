import React from 'react'
import { Icon, IconProps } from '../Icon'

export function IceCream04Icon({
  iconName = 'ice-cream-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
