import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoreVerticalCircle01Icon({
  iconName = 'more-vertical-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
