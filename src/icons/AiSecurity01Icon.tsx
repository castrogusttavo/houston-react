import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiSecurity01Icon({
  iconName = 'ai-security-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
