import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderLeft02Icon({
  iconName = 'border-left-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
