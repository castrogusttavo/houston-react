import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InsertRowDownIconIcon({ iconName = 'insert-row-down-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
