import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PeerToPeer03Icon({
  iconName = 'peer-to-peer-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
