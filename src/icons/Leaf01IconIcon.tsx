import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Leaf01IconIcon({ iconName = 'leaf-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
