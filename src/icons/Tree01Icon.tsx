import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tree01Icon({ iconName = 'tree-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
