import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WalletAdd02Icon({
  iconName = 'wallet-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
