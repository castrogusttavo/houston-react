import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Ticket03Icon({ iconName = 'ticket-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
