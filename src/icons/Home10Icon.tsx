import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Home10Icon({ iconName = 'home-10-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
