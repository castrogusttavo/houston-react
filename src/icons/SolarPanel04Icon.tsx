import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SolarPanel04Icon({
  iconName = 'solar-panel-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
