import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Wardrobe03Icon({
  iconName = 'wardrobe-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
