import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TongueWinkRightIcon({
  iconName = 'tongue-wink-right',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
