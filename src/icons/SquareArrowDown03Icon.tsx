import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDown03Icon({
  iconName = 'square-arrow-down-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
