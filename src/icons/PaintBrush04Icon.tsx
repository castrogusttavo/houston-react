import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PaintBrush04Icon({
  iconName = 'paint-brush-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
