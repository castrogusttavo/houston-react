import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragRight04Icon({
  iconName = 'drag-right-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
