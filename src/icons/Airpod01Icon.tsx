import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Airpod01Icon({ iconName = 'airpod-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
