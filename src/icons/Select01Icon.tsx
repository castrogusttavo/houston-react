import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Select01Icon({ iconName = 'select-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
