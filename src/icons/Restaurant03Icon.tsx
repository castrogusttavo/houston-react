import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Restaurant03Icon({ iconName = 'restaurant-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
