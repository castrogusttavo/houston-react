import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleUnlock02Icon({
  iconName = 'circle-unlock-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
