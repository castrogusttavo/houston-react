import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AirplaneTakeOff01Icon({
  iconName = 'airplane-take-off-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
