import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageNotification02Icon({
  iconName = 'message-notification-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
