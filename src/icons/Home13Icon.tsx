import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Home13Icon({ iconName = 'home-13-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
