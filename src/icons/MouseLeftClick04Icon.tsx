import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseLeftClick04Icon({
  iconName = 'mouse-left-click-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
