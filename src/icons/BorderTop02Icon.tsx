import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderTop02Icon({
  iconName = 'border-top-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
