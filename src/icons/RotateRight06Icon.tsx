import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateRight06Icon({
  iconName = 'rotate-right-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
