import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationOffline03Icon({
  iconName = 'location-offline-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
