import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallRinging04Icon({ iconName = 'call-ringing-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
