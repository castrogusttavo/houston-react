import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ChartBubble01Icon({
  iconName = 'chart-bubble-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
