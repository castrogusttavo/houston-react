import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowUp03Icon({
  iconName = 'square-arrow-up-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
