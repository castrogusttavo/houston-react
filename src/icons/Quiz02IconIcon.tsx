import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Quiz02IconIcon({ iconName = 'quiz-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
