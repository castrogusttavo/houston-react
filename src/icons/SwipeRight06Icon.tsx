import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeRight06Icon({
  iconName = 'swipe-right-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
