import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasket03Icon({
  iconName = 'shopping-basket-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
