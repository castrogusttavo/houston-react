import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DiceFaces06Icon({ iconName = 'dice-faces-06-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
