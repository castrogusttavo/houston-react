import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TextAlignRight01Icon({
  iconName = 'text-align-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
