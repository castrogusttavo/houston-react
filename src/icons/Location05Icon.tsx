import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location05Icon({
  iconName = 'location-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
