import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Directions02Icon({
  iconName = 'directions-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
