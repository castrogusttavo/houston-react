import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ComputerProgramming02Icon({
  iconName = 'computer-programming-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
