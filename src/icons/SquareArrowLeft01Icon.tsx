import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowLeft01Icon({
  iconName = 'square-arrow-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
