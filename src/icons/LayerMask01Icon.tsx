import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LayerMask01Icon({
  iconName = 'layer-mask-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
