import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartCheck01Icon({
  iconName = 'shopping-cart-check-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
