import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TextItalicSlashIconIcon({ iconName = 'text-italic-slash-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
