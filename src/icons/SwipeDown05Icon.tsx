import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown05Icon({
  iconName = 'swipe-down-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
