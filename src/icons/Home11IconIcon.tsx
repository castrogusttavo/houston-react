import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Home11IconIcon({ iconName = 'home-11-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
