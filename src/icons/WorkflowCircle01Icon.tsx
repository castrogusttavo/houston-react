import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowCircle01Icon({
  iconName = 'workflow-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
