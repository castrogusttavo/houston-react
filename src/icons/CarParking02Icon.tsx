import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CarParking02Icon({
  iconName = 'car-parking-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
