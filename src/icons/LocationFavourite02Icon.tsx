import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationFavourite02Icon({
  iconName = 'location-favourite-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
