import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TextAlignLeft01Icon({
  iconName = 'text-align-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
