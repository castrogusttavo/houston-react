import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Linkedin01Icon({
  iconName = 'linkedin-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
