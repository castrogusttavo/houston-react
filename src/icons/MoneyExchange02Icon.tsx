import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyExchange02Icon({
  iconName = 'money-exchange-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
