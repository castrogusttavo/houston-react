import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare07Icon({
  iconName = 'hierarchy-square-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
