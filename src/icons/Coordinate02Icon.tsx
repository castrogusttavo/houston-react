import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Coordinate02Icon({
  iconName = 'coordinate-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
