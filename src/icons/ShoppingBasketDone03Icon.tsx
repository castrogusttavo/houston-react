import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketDone03Icon({
  iconName = 'shopping-basket-done-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
