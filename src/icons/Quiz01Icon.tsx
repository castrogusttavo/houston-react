import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Quiz01Icon({ iconName = 'quiz-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
