import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationShare01Icon({
  iconName = 'location-share-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
