import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GoForward15SecIcon({
  iconName = 'go-forward-15-sec',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
