import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Toilet01Icon({ iconName = 'toilet-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
