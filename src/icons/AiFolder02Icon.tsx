import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiFolder02Icon({
  iconName = 'ai-folder-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
