import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Quiz04Icon({ iconName = 'quiz-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
