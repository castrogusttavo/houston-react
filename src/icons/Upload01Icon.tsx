import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Upload01Icon({ iconName = 'upload-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
