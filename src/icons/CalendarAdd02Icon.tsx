import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarAdd02Icon({
  iconName = 'calendar-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
