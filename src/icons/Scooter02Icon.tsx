import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Scooter02Icon({
  iconName = 'scooter-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
