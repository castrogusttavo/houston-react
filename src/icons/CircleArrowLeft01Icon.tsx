import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowLeft01Icon({
  iconName = 'circle-arrow-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
