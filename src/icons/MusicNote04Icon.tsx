import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MusicNote04Icon({
  iconName = 'music-note-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
