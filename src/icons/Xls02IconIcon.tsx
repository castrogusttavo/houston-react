import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Xls02IconIcon({ iconName = 'xls-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
