import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiEraserIcon({ iconName = 'ai-eraser-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
