import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ImageDelete02Icon({
  iconName = 'image-delete-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
