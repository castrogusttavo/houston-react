import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ChartLineData02Icon({
  iconName = 'chart-line-data-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
