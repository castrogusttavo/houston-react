import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WalletNotFound02Icon({
  iconName = 'wallet-not-found-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
