import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowRight02Icon({
  iconName = 'circle-arrow-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
