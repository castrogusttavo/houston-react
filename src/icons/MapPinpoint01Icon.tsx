import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapPinpoint01Icon({
  iconName = 'map-pinpoint-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
