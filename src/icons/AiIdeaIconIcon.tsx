import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiIdeaIconIcon({ iconName = 'ai-idea-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
