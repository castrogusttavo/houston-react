import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragRight02Icon({
  iconName = 'drag-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
