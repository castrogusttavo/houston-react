import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationOffline02Icon({
  iconName = 'location-offline-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
