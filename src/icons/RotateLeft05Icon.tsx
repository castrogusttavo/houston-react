import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateLeft05Icon({
  iconName = 'rotate-left-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
