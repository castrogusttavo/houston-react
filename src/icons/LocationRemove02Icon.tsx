import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationRemove02Icon({
  iconName = 'location-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
