import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Wallet03Icon({ iconName = 'wallet-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
