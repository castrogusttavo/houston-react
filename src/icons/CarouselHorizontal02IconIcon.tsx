import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CarouselHorizontal02IconIcon({ iconName = 'carousel-horizontal-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
