import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GameController03Icon({
  iconName = 'game-controller-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
