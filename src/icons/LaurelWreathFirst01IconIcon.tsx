import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LaurelWreathFirst01IconIcon({ iconName = 'laurel-wreath-first-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
