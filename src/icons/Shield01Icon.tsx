import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Shield01Icon({ iconName = 'shield-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
