import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Shield01Icon({ iconName = 'shield-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
