import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationOffline01Icon({
  iconName = 'location-offline-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
