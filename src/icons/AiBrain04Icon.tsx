import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiBrain04Icon({
  iconName = 'ai-brain-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
