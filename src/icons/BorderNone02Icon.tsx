import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderNone02Icon({
  iconName = 'border-none-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
