import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingDown03Icon({
  iconName = 'hand-pointing-down-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
