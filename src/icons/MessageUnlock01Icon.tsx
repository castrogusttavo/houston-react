import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageUnlock01Icon({
  iconName = 'message-unlock-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
