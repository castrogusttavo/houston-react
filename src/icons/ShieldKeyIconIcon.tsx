import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShieldKeyIconIcon({ iconName = 'shield-key-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
