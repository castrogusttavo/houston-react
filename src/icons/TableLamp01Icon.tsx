import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TableLamp01Icon({
  iconName = 'table-lamp-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
