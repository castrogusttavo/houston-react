import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DashboardSquare01Icon({
  iconName = 'dashboard-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
