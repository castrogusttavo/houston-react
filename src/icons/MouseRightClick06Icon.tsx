import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseRightClick06Icon({
  iconName = 'mouse-right-click-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
