import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare03Icon({
  iconName = 'hierarchy-square-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
