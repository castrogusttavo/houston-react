import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ImageAdd02Icon({
  iconName = 'image-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
