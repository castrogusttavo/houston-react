import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraOff02Icon({
  iconName = 'camera-off-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
