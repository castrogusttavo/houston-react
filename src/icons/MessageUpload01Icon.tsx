import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageUpload01Icon({
  iconName = 'message-upload-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
