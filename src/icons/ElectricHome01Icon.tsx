import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ElectricHome01Icon({
  iconName = 'electric-home-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
