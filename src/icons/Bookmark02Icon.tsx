import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Bookmark02Icon({
  iconName = 'bookmark-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
