import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Ramadhan01Icon({
  iconName = 'ramadhan-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
