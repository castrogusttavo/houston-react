import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Chemistry03Icon({
  iconName = 'chemistry-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
