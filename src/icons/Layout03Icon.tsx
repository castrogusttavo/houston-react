import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Layout03Icon({ iconName = 'layout-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
