import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockCheck01Icon({
  iconName = 'circle-lock-check-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
