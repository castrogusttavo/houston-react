import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare08Icon({
  iconName = 'workflow-square-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
