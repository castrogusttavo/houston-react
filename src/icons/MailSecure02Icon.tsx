import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailSecure02Icon({
  iconName = 'mail-secure-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
