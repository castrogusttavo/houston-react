import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorMagicSelection01Icon({
  iconName = 'cursor-magic-selection-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
