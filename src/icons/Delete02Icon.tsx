import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Delete02Icon({ iconName = 'delete-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
