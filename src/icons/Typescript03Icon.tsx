import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Typescript03Icon({
  iconName = 'typescript-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
