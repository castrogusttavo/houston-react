import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SettingError04Icon({
  iconName = 'setting-error-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
