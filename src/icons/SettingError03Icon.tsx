import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SettingError03Icon({
  iconName = 'setting-error-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
