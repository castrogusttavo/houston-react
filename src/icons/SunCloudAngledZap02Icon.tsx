import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudAngledZap02Icon({
  iconName = 'sun-cloud-angled-zap-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
