import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DollarReceive02Icon({
  iconName = 'dollar-receive-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
