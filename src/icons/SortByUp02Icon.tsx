import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SortByUp02Icon({
  iconName = 'sort-by-up-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
