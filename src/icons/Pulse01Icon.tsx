import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Pulse01Icon({ iconName = 'pulse-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
