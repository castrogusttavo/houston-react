import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Coupon01Icon({ iconName = 'coupon-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
