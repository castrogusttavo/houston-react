import React from 'react'
import { Icon, IconProps } from '../Icon'

export function KeyframesDoubleAddIcon({
  iconName = 'keyframes-double-add',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
