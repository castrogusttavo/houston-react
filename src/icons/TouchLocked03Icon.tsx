import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TouchLocked03Icon({
  iconName = 'touch-locked-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
