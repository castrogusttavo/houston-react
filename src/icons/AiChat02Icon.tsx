import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiChat02Icon({ iconName = 'ai-chat-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
