import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateRight02Icon({
  iconName = 'rotate-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
