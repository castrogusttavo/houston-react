import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Unlink06Icon({ iconName = 'unlink-06', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
