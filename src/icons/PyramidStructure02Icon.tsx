import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PyramidStructure02Icon({
  iconName = 'pyramid-structure-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
