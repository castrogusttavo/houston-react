import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BabyBed02Icon({
  iconName = 'baby-bed-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
