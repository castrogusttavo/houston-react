import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ElectricTower01Icon({
  iconName = 'electric-tower-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
