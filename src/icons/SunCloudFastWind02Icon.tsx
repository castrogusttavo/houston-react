import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudFastWind02Icon({
  iconName = 'sun-cloud-fast-wind-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
