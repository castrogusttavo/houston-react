import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Wallet01Icon({ iconName = 'wallet-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
