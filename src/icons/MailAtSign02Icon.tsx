import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailAtSign02Icon({
  iconName = 'mail-at-sign-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
