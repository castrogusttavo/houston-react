import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Target03Icon({ iconName = 'target-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
