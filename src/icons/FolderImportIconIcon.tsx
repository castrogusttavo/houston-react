import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FolderImportIconIcon({ iconName = 'folder-import-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
