import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight07Icon({
  iconName = 'pointing-right-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
