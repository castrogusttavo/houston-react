import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckUnread04IconIcon({ iconName = 'check-unread-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
