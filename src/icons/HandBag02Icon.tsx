import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandBag02Icon({
  iconName = 'hand-bag-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
