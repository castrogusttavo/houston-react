import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Camera01Icon({ iconName = 'camera-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
