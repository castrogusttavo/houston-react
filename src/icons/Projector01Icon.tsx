import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Projector01Icon({
  iconName = 'projector-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
