import React from 'react'
import { Icon, IconProps } from '../Icon'

export function UserLove01Icon({
  iconName = 'user-love-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
