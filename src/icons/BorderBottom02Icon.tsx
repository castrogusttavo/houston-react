import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderBottom02Icon({
  iconName = 'border-bottom-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
