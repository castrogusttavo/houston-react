import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckmarkSquare04Icon({
  iconName = 'checkmark-square-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
