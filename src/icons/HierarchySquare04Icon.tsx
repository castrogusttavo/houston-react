import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare04Icon({
  iconName = 'hierarchy-square-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
