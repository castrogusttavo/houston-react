import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowCircle02Icon({
  iconName = 'workflow-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
