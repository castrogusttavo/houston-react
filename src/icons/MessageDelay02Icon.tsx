import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageDelay02Icon({
  iconName = 'message-delay-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
