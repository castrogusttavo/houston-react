import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDown02Icon({
  iconName = 'arrow-down-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
