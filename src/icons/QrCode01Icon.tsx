import React from 'react'
import { Icon, IconProps } from '../Icon'

export function QrCode01Icon({ iconName = 'qr-code-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
