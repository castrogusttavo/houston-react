import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiProgrammingIconIcon({ iconName = 'ai-programming-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
