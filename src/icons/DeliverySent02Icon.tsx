import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliverySent02Icon({
  iconName = 'delivery-sent-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
