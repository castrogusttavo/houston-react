import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingRight02Icon({
  iconName = 'hand-pointing-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
