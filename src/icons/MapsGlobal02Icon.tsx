import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapsGlobal02Icon({
  iconName = 'maps-global-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
