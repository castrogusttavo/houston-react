import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Toilet02Icon({ iconName = 'toilet-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
