import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Logout05Icon({ iconName = 'logout-05', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
