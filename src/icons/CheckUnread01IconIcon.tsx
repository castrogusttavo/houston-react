import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckUnread01IconIcon({ iconName = 'check-unread-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
