import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudHailstone02Icon({
  iconName = 'sun-cloud-hailstone-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
