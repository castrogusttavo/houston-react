import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TimeSetting01Icon({
  iconName = 'time-setting-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
