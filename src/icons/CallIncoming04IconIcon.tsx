import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallIncoming04IconIcon({ iconName = 'call-incoming-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
