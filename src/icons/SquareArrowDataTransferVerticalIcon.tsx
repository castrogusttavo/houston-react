import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowDataTransferVerticalIcon({
  iconName = 'square-arrow-data-transfer-vertical',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
