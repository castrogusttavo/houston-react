import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationStar02Icon({
  iconName = 'location-star-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
