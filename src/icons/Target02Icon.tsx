import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Target02Icon({ iconName = 'target-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
