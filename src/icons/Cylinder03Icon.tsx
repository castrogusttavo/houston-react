import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cylinder03Icon({
  iconName = 'cylinder-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
