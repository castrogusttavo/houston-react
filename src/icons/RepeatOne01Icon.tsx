import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RepeatOne01Icon({
  iconName = 'repeat-one-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
