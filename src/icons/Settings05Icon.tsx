import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Settings05Icon({
  iconName = 'settings-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
