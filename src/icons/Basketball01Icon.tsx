import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Basketball01Icon({
  iconName = 'basketball-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
