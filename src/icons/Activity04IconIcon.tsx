import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Activity04IconIcon({ iconName = 'activity-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
