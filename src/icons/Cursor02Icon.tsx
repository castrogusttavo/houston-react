import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Cursor02Icon({ iconName = 'cursor-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
