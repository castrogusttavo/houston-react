import React from 'react'
import { Icon, IconProps } from '../Icon'

export function School01Icon({ iconName = 'school-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
