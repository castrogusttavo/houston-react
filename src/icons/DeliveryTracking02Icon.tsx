import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryTracking02Icon({
  iconName = 'delivery-tracking-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
