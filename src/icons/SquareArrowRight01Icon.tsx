import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowRight01Icon({
  iconName = 'square-arrow-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
