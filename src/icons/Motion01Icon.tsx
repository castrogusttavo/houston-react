import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Motion01Icon({ iconName = 'motion-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
