import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RepeatOne02Icon({
  iconName = 'repeat-one-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
