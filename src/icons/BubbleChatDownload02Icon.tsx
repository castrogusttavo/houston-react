import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BubbleChatDownload02Icon({
  iconName = 'bubble-chat-download-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
