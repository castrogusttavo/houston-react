import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AirplaneTakeOff02Icon({
  iconName = 'airplane-take-off-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
