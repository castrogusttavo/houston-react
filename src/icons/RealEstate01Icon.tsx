import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RealEstate01Icon({
  iconName = 'real-estate-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
