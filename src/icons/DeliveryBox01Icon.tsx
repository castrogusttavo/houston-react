import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryBox01Icon({
  iconName = 'delivery-box-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
