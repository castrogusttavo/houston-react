import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUp03Icon({
  iconName = 'arrow-up-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
