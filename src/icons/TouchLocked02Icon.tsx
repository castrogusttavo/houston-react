import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TouchLocked02Icon({
  iconName = 'touch-locked-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
