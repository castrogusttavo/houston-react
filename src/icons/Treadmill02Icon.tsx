import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Treadmill02Icon({
  iconName = 'treadmill-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
