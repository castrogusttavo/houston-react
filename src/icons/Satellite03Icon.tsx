import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Satellite03Icon({
  iconName = 'satellite-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
