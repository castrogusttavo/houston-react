import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DirectionLeft01Icon({
  iconName = 'direction-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
