import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoreHorizontalCircle02Icon({
  iconName = 'more-horizontal-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
