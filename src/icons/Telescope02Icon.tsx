import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Telescope02Icon({
  iconName = 'telescope-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
