import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyNotFound04Icon({
  iconName = 'money-not-found-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
