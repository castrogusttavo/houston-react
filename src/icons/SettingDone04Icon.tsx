import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SettingDone04Icon({
  iconName = 'setting-done-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
