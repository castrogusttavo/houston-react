import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Structure04Icon({
  iconName = 'structure-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
