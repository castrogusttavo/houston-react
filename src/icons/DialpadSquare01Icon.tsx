import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DialpadSquare01Icon({
  iconName = 'dialpad-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
