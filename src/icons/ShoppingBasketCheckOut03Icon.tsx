import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketCheckOut03Icon({
  iconName = 'shopping-basket-check-out-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
