import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDataTransferHorizontalIcon({
  iconName = 'circle-arrow-data-transfer-horizontal',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
