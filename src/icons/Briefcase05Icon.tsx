import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Briefcase05Icon({
  iconName = 'briefcase-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
