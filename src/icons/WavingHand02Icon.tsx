import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WavingHand02Icon({
  iconName = 'waving-hand-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
