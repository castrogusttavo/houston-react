import React from 'react'
import { Icon, IconProps } from '../Icon'

export function VictoryFinger01Icon({
  iconName = 'victory-finger-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
