import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiMail01Icon({ iconName = 'ai-mail-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
