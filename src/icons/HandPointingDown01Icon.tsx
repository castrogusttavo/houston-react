import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingDown01Icon({
  iconName = 'hand-pointing-down-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
