import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Configuration02Icon({
  iconName = 'configuration-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
