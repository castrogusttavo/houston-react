import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Resize02Icon({ iconName = 'resize-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
