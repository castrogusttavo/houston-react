import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallReceived02IconIcon({ iconName = 'call-received-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
