import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Blockchain07Icon({
  iconName = 'blockchain-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
