import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Knowledge01Icon({
  iconName = 'knowledge-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
