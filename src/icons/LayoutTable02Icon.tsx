import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LayoutTable02Icon({
  iconName = 'layout-table-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
