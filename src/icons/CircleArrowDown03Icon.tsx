import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDown03Icon({
  iconName = 'circle-arrow-down-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
