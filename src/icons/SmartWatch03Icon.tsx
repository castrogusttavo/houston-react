import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SmartWatch03Icon({
  iconName = 'smart-watch-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
