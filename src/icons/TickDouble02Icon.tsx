import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TickDouble02Icon({
  iconName = 'tick-double-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
