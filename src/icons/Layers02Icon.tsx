import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Layers02Icon({ iconName = 'layers-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
