import React from 'react'
import { Icon, IconProps } from '../Icon'

export function OnlineLearning04Icon({
  iconName = 'online-learning-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
