import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarCheckIn01Icon({
  iconName = 'calendar-check-in-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
