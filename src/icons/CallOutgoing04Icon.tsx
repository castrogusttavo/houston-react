import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallOutgoing04Icon({
  iconName = 'call-outgoing-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
