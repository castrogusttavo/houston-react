import React from 'react'
import { Icon, IconProps } from '../Icon'

export function VictoryFinger03Icon({
  iconName = 'victory-finger-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
