import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MinusPlusSquare01Icon({
  iconName = 'minus-plus-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
