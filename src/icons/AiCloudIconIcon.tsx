import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiCloudIconIcon({ iconName = 'ai-cloud-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
