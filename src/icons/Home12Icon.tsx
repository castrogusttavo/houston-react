import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Home12Icon({ iconName = 'home-12-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
