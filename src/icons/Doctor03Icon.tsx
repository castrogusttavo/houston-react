import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Doctor03Icon({ iconName = 'doctor-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
