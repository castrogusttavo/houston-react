import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorRectangleSelection01Icon({
  iconName = 'cursor-rectangle-selection-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
