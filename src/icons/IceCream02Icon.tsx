import React from 'react'
import { Icon, IconProps } from '../Icon'

export function IceCream02Icon({
  iconName = 'ice-cream-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
