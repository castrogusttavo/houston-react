import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AiVideoIconIcon({ iconName = 'ai-video-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
