import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SecondBracketSquareIcon({
  iconName = '2nd-bracket-square',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
