import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Presentation07Icon({
  iconName = 'presentation-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
