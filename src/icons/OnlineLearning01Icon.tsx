import React from 'react'
import { Icon, IconProps } from '../Icon'

export function OnlineLearning01Icon({
  iconName = 'online-learning-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
