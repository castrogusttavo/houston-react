import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DialpadCircle01Icon({
  iconName = 'dialpad-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
