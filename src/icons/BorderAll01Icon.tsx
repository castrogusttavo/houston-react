import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderAll01Icon({
  iconName = 'border-all-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
