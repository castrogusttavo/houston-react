import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location03Icon({
  iconName = 'location-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
