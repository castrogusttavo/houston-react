import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight04Icon({
  iconName = 'pointing-right-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
