import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Castle02Icon({ iconName = 'castle-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
