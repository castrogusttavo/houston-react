import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DashboardSquare03Icon({
  iconName = 'dashboard-square-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
