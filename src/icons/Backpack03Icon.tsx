import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Backpack03Icon({
  iconName = 'backpack-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
