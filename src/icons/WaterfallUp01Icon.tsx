import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WaterfallUp01Icon({
  iconName = 'waterfall-up-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
