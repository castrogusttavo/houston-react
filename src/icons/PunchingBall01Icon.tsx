import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PunchingBall01Icon({
  iconName = 'punching-ball-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
