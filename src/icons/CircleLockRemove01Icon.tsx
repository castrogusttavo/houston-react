import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockRemove01Icon({
  iconName = 'circle-lock-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
