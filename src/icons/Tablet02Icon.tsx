import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Tablet02Icon({ iconName = 'tablet-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
