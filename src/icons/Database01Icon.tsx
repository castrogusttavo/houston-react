import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Database01Icon({
  iconName = 'database-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
