import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LockSync02Icon({
  iconName = 'lock-sync-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
