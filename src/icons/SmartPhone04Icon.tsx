import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SmartPhone04Icon({
  iconName = 'smart-phone-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
