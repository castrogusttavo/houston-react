import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WindPower03Icon({
  iconName = 'wind-power-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
