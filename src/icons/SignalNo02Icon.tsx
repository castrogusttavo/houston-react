import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SignalNo02Icon({
  iconName = 'signal-no-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
