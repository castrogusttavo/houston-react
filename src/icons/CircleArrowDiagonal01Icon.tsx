import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDiagonal01Icon({
  iconName = 'circle-arrow-diagonal-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
