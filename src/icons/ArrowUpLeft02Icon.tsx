import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowUpLeft02Icon({
  iconName = 'arrow-up-left-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
