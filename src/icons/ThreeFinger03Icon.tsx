import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ThreeFinger03Icon({
  iconName = 'three-finger-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
