import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallIncoming04Icon({
  iconName = 'call-incoming-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
