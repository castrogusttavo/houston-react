import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown06Icon({
  iconName = 'swipe-down-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
