import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft03Icon({
  iconName = 'swipe-left-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
