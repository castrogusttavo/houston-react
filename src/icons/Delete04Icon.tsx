import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Delete04Icon({ iconName = 'delete-04', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
