import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraSmile01Icon({
  iconName = 'camera-smile-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
