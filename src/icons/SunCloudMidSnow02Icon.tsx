import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudMidSnow02Icon({
  iconName = 'sun-cloud-mid-snow-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
