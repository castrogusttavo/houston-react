import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CursorCircleSelection02Icon({
  iconName = 'cursor-circle-selection-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
