import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Restaurant01IconIcon({ iconName = 'restaurant-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
