import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ElectricTower02Icon({
  iconName = 'electric-tower-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
