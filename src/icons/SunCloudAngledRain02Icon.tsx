import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudAngledRain02Icon({
  iconName = 'sun-cloud-angled-rain-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
