import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft02Icon({
  iconName = 'swipe-left-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
