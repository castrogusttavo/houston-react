import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleUnlock01Icon({
  iconName = 'circle-unlock-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
