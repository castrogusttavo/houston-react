import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ImageDone02Icon({
  iconName = 'image-done-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
