import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Calling02Icon({ iconName = 'calling-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
