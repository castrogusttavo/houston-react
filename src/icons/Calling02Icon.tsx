import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Calling02Icon({
  iconName = 'calling-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
