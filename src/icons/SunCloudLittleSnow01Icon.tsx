import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudLittleSnow01Icon({
  iconName = 'sun-cloud-little-snow-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
