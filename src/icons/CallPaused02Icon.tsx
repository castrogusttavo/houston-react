import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallPaused02Icon({ iconName = 'call-paused-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
