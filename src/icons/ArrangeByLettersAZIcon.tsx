import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrangeByLettersAZIcon({
  iconName = 'arrange-by-letters-a-z',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
