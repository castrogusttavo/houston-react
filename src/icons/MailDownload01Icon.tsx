import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailDownload01Icon({
  iconName = 'mail-download-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
