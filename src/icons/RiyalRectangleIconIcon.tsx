import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RiyalRectangleIconIcon({ iconName = 'riyal-rectangle-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
