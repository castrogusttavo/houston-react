import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HighHeels02Icon({
  iconName = 'high-heels-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
