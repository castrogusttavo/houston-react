import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleLockRemove02Icon({
  iconName = 'circle-lock-remove-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
