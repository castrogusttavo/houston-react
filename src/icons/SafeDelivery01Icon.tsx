import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SafeDelivery01Icon({
  iconName = 'safe-delivery-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
