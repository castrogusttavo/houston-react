import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyReceiveFlow02Icon({
  iconName = 'money-receive-flow-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
