import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Calendar02Icon({
  iconName = 'calendar-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
