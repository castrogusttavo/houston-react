import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneySendFlow02Icon({
  iconName = 'money-send-flow-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
