import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Coupon03Icon({ iconName = 'coupon-03', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
