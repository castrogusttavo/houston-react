import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PlayListFavourite02Icon({
  iconName = 'play-list-favourite-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
