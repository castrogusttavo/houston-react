import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft08Icon({
  iconName = 'pointing-left-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
