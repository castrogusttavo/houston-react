import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TwoFinger05Icon({
  iconName = 'two-finger-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
