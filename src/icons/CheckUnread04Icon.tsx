import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckUnread04Icon({
  iconName = 'check-unread-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
