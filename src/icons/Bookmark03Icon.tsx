import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Bookmark03Icon({
  iconName = 'bookmark-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
