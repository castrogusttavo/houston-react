import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TaskDaily02Icon({
  iconName = 'task-daily-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
