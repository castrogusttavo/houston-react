import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InsertColumnIconIcon({ iconName = 'insert-column-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
