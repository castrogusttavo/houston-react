import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarLove01Icon({
  iconName = 'calendar-love-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
