import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Diamond02Icon({
  iconName = 'diamond-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
