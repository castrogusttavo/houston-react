import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DownloadSquare01Icon({
  iconName = 'download-square-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
