import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Plant02IconIcon({ iconName = 'plant-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
