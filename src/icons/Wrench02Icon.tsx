import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Wrench02Icon({ iconName = 'wrench-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
