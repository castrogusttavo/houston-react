import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Unlink02Icon({ iconName = 'unlink-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
