import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeUp02Icon({
  iconName = 'swipe-up-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
