import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ComputerDesk03Icon({
  iconName = 'computer-desk-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
