import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Saturn01Icon({ iconName = 'saturn-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
