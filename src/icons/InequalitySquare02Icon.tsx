import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InequalitySquare02Icon({
  iconName = 'inequality-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
