import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageMultiple02Icon({
  iconName = 'message-multiple-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
