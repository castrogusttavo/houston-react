import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowCircle03Icon({
  iconName = 'workflow-circle-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
