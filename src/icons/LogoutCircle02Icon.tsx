import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LogoutCircle02Icon({
  iconName = 'logout-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
