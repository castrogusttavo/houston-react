import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Disability02Icon({
  iconName = 'disability-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
