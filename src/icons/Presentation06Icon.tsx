import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Presentation06Icon({
  iconName = 'presentation-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
