import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallDisabled02Icon({ iconName = 'call-disabled-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
