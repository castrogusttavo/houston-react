import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeRight02Icon({
  iconName = 'swipe-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
