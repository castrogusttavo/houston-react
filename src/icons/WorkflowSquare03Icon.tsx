import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare03Icon({
  iconName = 'workflow-square-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
