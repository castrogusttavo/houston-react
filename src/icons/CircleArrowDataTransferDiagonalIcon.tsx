import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowDataTransferDiagonalIcon({
  iconName = 'circle-arrow-data-transfer-diagonal',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
