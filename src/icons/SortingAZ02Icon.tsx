import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SortingAZ02Icon({
  iconName = 'sorting-a-z-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
