import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Presentation04Icon({
  iconName = 'presentation-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
