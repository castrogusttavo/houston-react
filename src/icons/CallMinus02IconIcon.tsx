import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallMinus02IconIcon({ iconName = 'call-minus-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
