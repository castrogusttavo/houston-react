import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mining01Icon({ iconName = 'mining-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
