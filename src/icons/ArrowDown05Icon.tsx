import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDown05Icon({
  iconName = 'arrow-down-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
