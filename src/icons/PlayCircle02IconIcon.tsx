import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PlayCircle02IconIcon({ iconName = 'play-circle-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
