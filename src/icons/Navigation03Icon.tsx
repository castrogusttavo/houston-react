import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Navigation03Icon({
  iconName = 'navigation-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
