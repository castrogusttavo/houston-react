import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ElectricHome02Icon({
  iconName = 'electric-home-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
