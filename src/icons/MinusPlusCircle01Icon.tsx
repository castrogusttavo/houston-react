import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MinusPlusCircle01Icon({
  iconName = 'minus-plus-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
