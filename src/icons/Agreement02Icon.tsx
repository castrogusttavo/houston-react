import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Agreement02Icon({
  iconName = 'agreement-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
