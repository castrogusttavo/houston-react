import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Playlist03Icon({
  iconName = 'playlist-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
