import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft08Icon({
  iconName = 'swipe-left-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
