import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Rhombus01Icon({
  iconName = 'rhombus-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
