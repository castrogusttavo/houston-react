import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowUp02Icon({
  iconName = 'circle-arrow-up-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
