import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HandPointingLeft03Icon({
  iconName = 'hand-pointing-left-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
