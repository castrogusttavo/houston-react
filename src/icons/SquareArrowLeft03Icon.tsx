import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowLeft03Icon({
  iconName = 'square-arrow-left-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
