import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InequalityCircle02Icon({
  iconName = 'inequality-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
