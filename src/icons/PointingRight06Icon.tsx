import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingRight06Icon({
  iconName = 'pointing-right-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
