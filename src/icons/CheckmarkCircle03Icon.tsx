import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckmarkCircle03Icon({
  iconName = 'checkmark-circle-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
