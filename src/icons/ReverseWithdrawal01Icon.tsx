import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ReverseWithdrawal01Icon({
  iconName = 'reverse-withdrawal-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
