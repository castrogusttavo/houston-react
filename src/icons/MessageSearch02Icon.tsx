import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageSearch02Icon({
  iconName = 'message-search-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
