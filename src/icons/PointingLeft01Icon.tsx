import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft01Icon({
  iconName = 'pointing-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
