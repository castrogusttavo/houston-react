import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationOffline04Icon({
  iconName = 'location-offline-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
