import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowReload02Icon({
  iconName = 'square-arrow-reload-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
