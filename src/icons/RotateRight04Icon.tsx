import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateRight04Icon({
  iconName = 'rotate-right-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
