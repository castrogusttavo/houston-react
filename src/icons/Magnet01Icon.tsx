import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Magnet01Icon({ iconName = 'magnet-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
