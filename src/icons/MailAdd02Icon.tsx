import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailAdd02Icon({
  iconName = 'mail-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
