import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareArrowShrink02Icon({
  iconName = 'square-arrow-shrink-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
