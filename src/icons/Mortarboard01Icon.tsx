import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Mortarboard01Icon({
  iconName = 'mortarboard-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
