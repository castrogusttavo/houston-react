import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MapsGlobal01Icon({
  iconName = 'maps-global-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
