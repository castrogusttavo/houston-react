import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloud02Icon({
  iconName = 'sun-cloud-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
