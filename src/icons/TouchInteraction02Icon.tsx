import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TouchInteraction02Icon({
  iconName = 'touch-interaction-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
