import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InternetAntenna02Icon({
  iconName = 'internet-antenna-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
