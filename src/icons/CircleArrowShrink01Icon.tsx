import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowShrink01Icon({
  iconName = 'circle-arrow-shrink-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
