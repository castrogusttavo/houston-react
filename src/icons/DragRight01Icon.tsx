import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragRight01Icon({
  iconName = 'drag-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
