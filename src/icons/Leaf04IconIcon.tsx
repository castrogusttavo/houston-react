import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Leaf04IconIcon({ iconName = 'leaf-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
