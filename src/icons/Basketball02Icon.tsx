import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Basketball02Icon({
  iconName = 'basketball-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
