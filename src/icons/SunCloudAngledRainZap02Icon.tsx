import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudAngledRainZap02Icon({
  iconName = 'sun-cloud-angled-rain-zap-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
