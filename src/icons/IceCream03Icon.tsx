import React from 'react'
import { Icon, IconProps } from '../Icon'

export function IceCream03Icon({
  iconName = 'ice-cream-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
