import React from 'react'
import { Icon, IconProps } from '../Icon'

export function StoreLocation02Icon({
  iconName = 'store-location-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
