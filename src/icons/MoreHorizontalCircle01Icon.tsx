import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoreHorizontalCircle01Icon({
  iconName = 'more-horizontal-circle-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
