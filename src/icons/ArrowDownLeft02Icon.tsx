import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDownLeft02Icon({
  iconName = 'arrow-down-left-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
