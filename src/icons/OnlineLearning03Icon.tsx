import React from 'react'
import { Icon, IconProps } from '../Icon'

export function OnlineLearning03Icon({
  iconName = 'online-learning-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
