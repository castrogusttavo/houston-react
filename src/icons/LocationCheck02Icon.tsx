import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationCheck02Icon({
  iconName = 'location-check-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
