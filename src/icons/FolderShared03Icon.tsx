import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FolderShared03Icon({
  iconName = 'folder-shared-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
