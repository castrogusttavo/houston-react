import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallOutgoing04IconIcon({ iconName = 'call-outgoing-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
