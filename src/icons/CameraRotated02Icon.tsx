import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CameraRotated02Icon({
  iconName = 'camera-rotated-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
