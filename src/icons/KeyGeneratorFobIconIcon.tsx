import React from 'react'
import { Icon, IconProps } from '../Icon'

export function KeyGeneratorFobIconIcon({ iconName = 'key-generator-fob-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
