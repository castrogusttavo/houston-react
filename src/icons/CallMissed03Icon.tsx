import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallMissed03Icon({
  iconName = 'call-missed-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
