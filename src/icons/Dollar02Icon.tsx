import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Dollar02Icon({ iconName = 'dollar-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
