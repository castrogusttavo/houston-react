import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CentralShaheedMinarIcon({
  iconName = 'central-shaheed-minar',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
