import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragLeft01Icon({
  iconName = 'drag-left-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
