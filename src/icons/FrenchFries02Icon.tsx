import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FrenchFries02Icon({
  iconName = 'french-fries-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
