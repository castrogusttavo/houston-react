import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Satellite02Icon({
  iconName = 'satellite-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
