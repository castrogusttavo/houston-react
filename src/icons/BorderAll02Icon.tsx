import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BorderAll02Icon({
  iconName = 'border-all-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
