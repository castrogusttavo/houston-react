import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft09Icon({
  iconName = 'swipe-left-09',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
