import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingCartFavorite02Icon({
  iconName = 'shopping-cart-favorite-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
