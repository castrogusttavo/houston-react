import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PaintBrush03Icon({
  iconName = 'paint-brush-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
