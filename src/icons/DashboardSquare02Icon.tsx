import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DashboardSquare02Icon({
  iconName = 'dashboard-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
