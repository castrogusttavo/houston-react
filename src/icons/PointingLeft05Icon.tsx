import React from 'react'
import { Icon, IconProps } from '../Icon'

export function PointingLeft05Icon({
  iconName = 'pointing-left-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
