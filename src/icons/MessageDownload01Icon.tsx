import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MessageDownload01Icon({
  iconName = 'message-download-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
