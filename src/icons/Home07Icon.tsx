import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Home07Icon({ iconName = 'home-07-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
