import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDown03Icon({
  iconName = 'arrow-down-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
