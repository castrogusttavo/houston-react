import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketRemove01Icon({
  iconName = 'shopping-basket-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
