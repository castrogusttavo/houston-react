import React from 'react'
import { Icon, IconProps } from '../Icon'

export function WorkflowSquare05Icon({
  iconName = 'workflow-square-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
