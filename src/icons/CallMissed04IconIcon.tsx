import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CallMissed04IconIcon({ iconName = 'call-missed-04-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
