import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowRight02Icon({
  iconName = 'arrow-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
