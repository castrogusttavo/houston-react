import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Setting07Icon({
  iconName = 'setting-07',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
