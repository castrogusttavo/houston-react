import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkMinus02Icon({
  iconName = 'bookmark-minus-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
