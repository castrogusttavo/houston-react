import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Sorting05Icon({
  iconName = 'sorting-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
