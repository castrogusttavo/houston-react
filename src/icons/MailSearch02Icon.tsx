import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MailSearch02Icon({
  iconName = 'mail-search-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
