import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowDownRight02Icon({
  iconName = 'arrow-down-right-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
