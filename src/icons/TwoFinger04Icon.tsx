import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TwoFinger04Icon({
  iconName = 'two-finger-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
