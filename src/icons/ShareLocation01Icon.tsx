import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShareLocation01Icon({
  iconName = 'share-location-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
