import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SchoolBell01Icon({
  iconName = 'school-bell-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
