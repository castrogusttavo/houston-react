import React from 'react'
import { Icon, IconProps } from '../Icon'

export function InternetAntenna04Icon({
  iconName = 'internet-antenna-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
