import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BookmarkRemove01Icon({
  iconName = 'bookmark-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
