import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LinkCircle02Icon({
  iconName = 'link-circle-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
