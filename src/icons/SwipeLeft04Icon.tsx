import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeLeft04Icon({
  iconName = 'swipe-left-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
