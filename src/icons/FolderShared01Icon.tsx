import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FolderShared01Icon({
  iconName = 'folder-shared-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
