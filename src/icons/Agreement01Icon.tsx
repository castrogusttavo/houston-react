import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Agreement01Icon({
  iconName = 'agreement-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
