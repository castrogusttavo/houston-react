import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CheckmarkSquare03Icon({
  iconName = 'checkmark-square-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
