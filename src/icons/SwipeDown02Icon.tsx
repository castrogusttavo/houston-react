import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeDown02Icon({
  iconName = 'swipe-down-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
