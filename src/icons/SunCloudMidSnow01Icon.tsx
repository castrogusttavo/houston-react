import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudMidSnow01Icon({
  iconName = 'sun-cloud-mid-snow-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
