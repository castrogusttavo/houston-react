import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ReverseWithdrawal01IconIcon({ iconName = 'reverse-withdrawal-01-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
