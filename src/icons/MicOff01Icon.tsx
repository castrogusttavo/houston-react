import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MicOff01Icon({ iconName = 'mic-off-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
