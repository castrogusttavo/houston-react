import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BitcoinSmartphone02Icon({
  iconName = 'bitcoin-smartphone-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
