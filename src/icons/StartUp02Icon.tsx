import React from 'react'
import { Icon, IconProps } from '../Icon'

export function StartUp02Icon({
  iconName = 'start-up-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
