import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseRightClick04Icon({
  iconName = 'mouse-right-click-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
