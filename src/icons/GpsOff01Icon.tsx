import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GpsOff01Icon({ iconName = 'gps-off-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
