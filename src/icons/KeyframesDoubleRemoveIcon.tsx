import React from 'react'
import { Icon, IconProps } from '../Icon'

export function KeyframesDoubleRemoveIcon({
  iconName = 'keyframes-double-remove',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
