import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ThreeFinger02Icon({
  iconName = 'three-finger-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
