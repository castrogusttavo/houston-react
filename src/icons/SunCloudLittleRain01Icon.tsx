import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudLittleRain01Icon({
  iconName = 'sun-cloud-little-rain-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
