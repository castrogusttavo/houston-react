import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowLeft04Icon({
  iconName = 'arrow-left-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
