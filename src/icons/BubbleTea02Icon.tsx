import React from 'react'
import { Icon, IconProps } from '../Icon'

export function BubbleTea02Icon({
  iconName = 'bubble-tea-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
