import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HierarchySquare08Icon({
  iconName = 'hierarchy-square-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
