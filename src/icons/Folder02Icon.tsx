import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Folder02Icon({ iconName = 'folder-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
