import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CalendarUpload02Icon({
  iconName = 'calendar-upload-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
