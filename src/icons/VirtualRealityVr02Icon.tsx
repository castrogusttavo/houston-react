import React from 'react'
import { Icon, IconProps } from '../Icon'

export function VirtualRealityVr02Icon({
  iconName = 'virtual-reality-vr-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
