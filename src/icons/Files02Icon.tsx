import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Files02Icon({ iconName = 'files-02-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
