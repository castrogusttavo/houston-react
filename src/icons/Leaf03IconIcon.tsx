import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Leaf03IconIcon({ iconName = 'leaf-03-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
