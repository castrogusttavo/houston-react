import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ArrowLeft03Icon({
  iconName = 'arrow-left-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
