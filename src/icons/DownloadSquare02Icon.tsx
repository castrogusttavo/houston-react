import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DownloadSquare02Icon({
  iconName = 'download-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
