import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CommentAdd02Icon({
  iconName = 'comment-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
