import React from 'react'
import { Icon, IconProps } from '../Icon'

export function CircleArrowLeft03Icon({
  iconName = 'circle-arrow-left-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
