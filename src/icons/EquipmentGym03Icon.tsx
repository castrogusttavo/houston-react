import React from 'react'
import { Icon, IconProps } from '../Icon'

export function EquipmentGym03Icon({
  iconName = 'equipment-gym-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
