import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryView01Icon({
  iconName = 'delivery-view-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
