import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DragLeft03Icon({
  iconName = 'drag-left-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
