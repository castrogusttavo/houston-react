import React from 'react'
import { Icon, IconProps } from '../Icon'

export function GoogleGeminiIconIcon({ iconName = 'google-gemini-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
