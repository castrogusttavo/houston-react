import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Touchpad04Icon({
  iconName = 'touchpad-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
