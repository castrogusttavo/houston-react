import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SaveMoneyPoundIconIcon({ iconName = 'save-money-pound-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
