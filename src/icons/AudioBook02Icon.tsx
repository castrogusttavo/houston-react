import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AudioBook02Icon({
  iconName = 'audio-book-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
