import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeRight08Icon({
  iconName = 'swipe-right-08',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
