import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyRemove01Icon({
  iconName = 'money-remove-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
