import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseLeftClick05Icon({
  iconName = 'mouse-left-click-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
