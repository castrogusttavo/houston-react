import React from 'react'
import { Icon, IconProps } from '../Icon'

export function LocationUser03Icon({
  iconName = 'location-user-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
