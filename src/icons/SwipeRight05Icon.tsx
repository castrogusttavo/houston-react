import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SwipeRight05Icon({
  iconName = 'swipe-right-05',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
