import React from 'react'
import { Icon, IconProps } from '../Icon'

export function FrenchFries01Icon({
  iconName = 'french-fries-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
