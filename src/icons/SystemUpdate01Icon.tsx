import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SystemUpdate01Icon({
  iconName = 'system-update-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
