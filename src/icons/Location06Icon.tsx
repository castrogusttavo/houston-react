import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Location06Icon({
  iconName = 'location-06',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
