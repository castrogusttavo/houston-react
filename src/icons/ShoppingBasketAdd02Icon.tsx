import React from 'react'
import { Icon, IconProps } from '../Icon'

export function ShoppingBasketAdd02Icon({
  iconName = 'shopping-basket-add-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
