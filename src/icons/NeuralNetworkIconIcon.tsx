import React from 'react'
import { Icon, IconProps } from '../Icon'

export function NeuralNetworkIconIcon({ iconName = 'neural-network-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
