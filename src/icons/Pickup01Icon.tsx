import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Pickup01Icon({ iconName = 'pickup-01', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
