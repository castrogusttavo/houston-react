import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SignalFull01Icon({
  iconName = 'signal-full-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
