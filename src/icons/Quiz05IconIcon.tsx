import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Quiz05IconIcon({ iconName = 'quiz-05-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
