import React from 'react'
import { Icon, IconProps } from '../Icon'

export function RotateLeft02Icon({
  iconName = 'rotate-left-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
