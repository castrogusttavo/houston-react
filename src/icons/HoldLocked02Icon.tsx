import React from 'react'
import { Icon, IconProps } from '../Icon'

export function HoldLocked02Icon({
  iconName = 'hold-locked-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
