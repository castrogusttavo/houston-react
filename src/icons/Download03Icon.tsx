import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Download03Icon({
  iconName = 'download-03',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
