import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DeliveryView02Icon({
  iconName = 'delivery-view-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
