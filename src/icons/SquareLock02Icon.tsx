import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SquareLock02Icon({
  iconName = 'square-lock-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
