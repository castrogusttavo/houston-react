import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Forward02Icon({
  iconName = 'forward-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
