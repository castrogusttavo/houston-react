import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MouseRightClick02Icon({
  iconName = 'mouse-right-click-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
