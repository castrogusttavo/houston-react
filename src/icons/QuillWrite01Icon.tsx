import React from 'react'
import { Icon, IconProps } from '../Icon'

export function QuillWrite01Icon({
  iconName = 'quill-write-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
