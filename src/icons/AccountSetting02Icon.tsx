import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AccountSetting02Icon({
  iconName = 'account-setting-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
