import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MusicNoteSquare02Icon({
  iconName = 'music-note-square-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
