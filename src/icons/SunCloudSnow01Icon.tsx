import React from 'react'
import { Icon, IconProps } from '../Icon'

export function SunCloudSnow01Icon({
  iconName = 'sun-cloud-snow-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
