import React from 'react'
import { Icon, IconProps } from '../Icon'

export function MoneyNotFound02Icon({
  iconName = 'money-not-found-02',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
