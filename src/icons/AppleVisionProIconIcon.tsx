import React from 'react'
import { Icon, IconProps } from '../Icon'

export function AppleVisionProIconIcon({ iconName = 'apple-vision-pro-icon', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
