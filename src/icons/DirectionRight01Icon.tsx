import React from 'react'
import { Icon, IconProps } from '../Icon'

export function DirectionRight01Icon({
  iconName = 'direction-right-01',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
