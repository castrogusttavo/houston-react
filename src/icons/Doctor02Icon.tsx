import React from 'react'
import { Icon, IconProps } from '../Icon'

export function Doctor02Icon({ iconName = 'doctor-02', ...props }: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
