import React from 'react'
import { Icon, IconProps } from '../Icon'

export function TouchLocked04Icon({
  iconName = 'touch-locked-04',
  ...props
}: IconProps) {
  return <Icon {...props} iconName={iconName} />
}
